# Force pnpm usage
engine-strict=true

# Hoist dependencies to root node_modules (better compatibility with Next.js)
node-linker=hoisted

# Automatically install peer dependencies
auto-install-peers=true

# Use strict peer dependencies
strict-peer-dependencies=false

